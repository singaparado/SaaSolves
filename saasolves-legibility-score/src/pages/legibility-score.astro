---
/**
 * RECOVERY STATUS: HIGH CONFIDENCE
 *
 * Reconstructed from rendered HTML metadata, canonical URL structure,
 * compiled import pattern (z = Nav, R = HeroSection, E = Footer, B = DiagnosticPanel),
 * and all recovered meta tag content.
 *
 * The original Astro frontmatter compiled away in production — this
 * reconstruction infers the logical structure from surrounding evidence.
 */

import { DiagnosticPanel } from "../components/diagnostic";

const META = {
  title: "The Legibility Score — SaaSolves",
  description:
    "Score your SaaS homepage hero copy against the Three Laws of Translation — Tangibility, Intuitiveness, Sustainability — and the 5-second Interpretation Window. A free narrative diagnostic from SaaSolves.",
  canonical: "https://saasolves.vercel.app/legibility-score",
  ogTitle: "The Legibility Score — SaaSolves",
  ogDescription:
    "Paste your hero copy. Get a 0–100 legibility score and see exactly where your narrative fractures — in seconds, free.",
  ogImage: "https://saasolves.vercel.app/og-image.png",
  author: "Mahdi Singaparado",
};

const HERO = {
  kicker: "Free Diagnostic // The Legibility Score",
  headlineLead:
    "Your product is legible to you. The question is whether it's legible to a ",
  headlineAccent: "stranger.",
  segments: [
    "74% of visitors leave when meaning isn't clear at first contact.",
    "Score your hero copy against the Three Laws in seconds — then see exactly where it fractures.",
  ],
  primaryCta: {
    label: "Run the Diagnostic →",
    href: "#diagnostic",
  },
  secondaryCta: {
    label: "How scoring works",
    href: "#diagnostic",
  },
  ctaNote: "No signup. No call. Just the read the market already gives you.",
};
---

<html
  lang="en"
  class="text-ploy-neutral-inverse-50 decoration-white w-full relative overflow-x-hidden outline-white dark"
  data-theme="dark"
>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <title>{META.title}</title>
    <meta name="description" content={META.description} />
    <meta name="author" content={META.author} />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href={META.canonical} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={META.ogTitle} />
    <meta property="og:description" content={META.ogDescription} />
    <meta property="og:image" content={META.ogImage} />
    <meta property="og:url" content={META.canonical} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={META.ogTitle} />

    <!-- Schema.org -->
    <script type="application/ld+json">
      {
        JSON.stringify({
          "@context": "https://schema.org",
          "@graph": [
            {
              "@type": "Organization",
              name: "SaaSolves",
              url: "https://saasolves.vercel.app",
            },
            {
              "@type": "WebPage",
              name: META.title,
              url: META.canonical,
              description: META.description,
              author: {
                "@type": "Person",
                name: META.author,
              },
            },
            {
              "@type": "SoftwareApplication",
              name: "The Legibility Score",
              applicationCategory: "BusinessApplication",
              description: META.description,
              url: META.canonical,
              offers: {
                "@type": "Offer",
                price: "0",
                priceCurrency: "USD",
              },
            },
          ],
        })
      }
    </script>
  </head>

  <body>
    <!--
      Nav and Footer components are referenced from the compiled bundle
      as imports z (Nav) and E (Footer) from footer.pYR9_Y61.js.
      These need to be reconstructed separately from the main SaaSolves
      site codebase or the footer bundle if recovered.

      RECOVERY STATUS: INFERRED
    -->

    <!-- Nav placeholder — reconstruct from main site -->
    <!-- <Nav /> -->

    <div class="w-full min-h-screen relative z-[1]">

      <!--
        HeroSection — reconstructed from compiled R component props.
        All prop values directly recovered from compiled bundle.
        RECOVERY STATUS: DIRECTLY RECOVERED (props) / INFERRED (component structure)
      -->
      <section class="hero-section px-[6vw] py-[14vh]">
        <div class="max-w-[62.5rem] mx-auto">
          <div class="font-button text-ploy-accent-primary-500 text-xs tracking-widest uppercase mb-5">
            {HERO.kicker}
          </div>
          <h1 class="font-heading font-bold leading-[1.08] mb-7 max-md:text-4xl lg:text-6xl max-w-[52rem]">
            {HERO.headlineLead}
            <span class="text-ploy-accent-primary">{HERO.headlineAccent}</span>
          </h1>
          <div class="flex flex-col gap-3 mb-10 max-w-[42rem]">
            {HERO.segments.map((segment) => (
              <p class="text-ploy-text-secondary leading-[1.75] text-base">
                {segment}
              </p>
            ))}
          </div>
          <div class="flex flex-wrap gap-4 items-center">
            <a
              href={HERO.primaryCta.href}
              class="font-button border-solid border-ploy-button-primary-border bg-ploy-background-accent-primary text-ploy-button-primary-text font-semibold text-xs tracking-[0.08em] uppercase inline-flex items-center gap-2 px-[1.625rem] py-3 rounded-xl hover:[translate:0_-2px] hover:shadow-[0_18px_48px_rgba(0,0,0,0.45)] border transition-all duration-[0.25s]"
            >
              {HERO.primaryCta.label}
            </a>
            <a
              href={HERO.secondaryCta.href}
              class="font-button text-ploy-text-secondary text-xs tracking-[0.08em] uppercase hover:text-ploy-text-primary transition-colors duration-200"
            >
              {HERO.secondaryCta.label}
            </a>
          </div>
          <p class="font-button text-ploy-neutral-primary-700 text-[0.6rem] tracking-wider uppercase mt-5">
            {HERO.ctaNote}
          </p>
        </div>
      </section>

      <!-- Primary diagnostic interface — React island -->
      <DiagnosticPanel client:load />

    </div>

    <!-- Footer placeholder — reconstruct from main site -->
    <!-- <Footer /> -->
  </body>
</html>
